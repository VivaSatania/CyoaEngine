{
  "name": "cyoa-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/packages/engine-core/test/*.test.js",
    "check": "npm test"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.4"
  }
}
